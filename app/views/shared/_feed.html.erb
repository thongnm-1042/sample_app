<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items,
    params: {controller: :static_pages, action: :home},
    theme: Settings.theme.name_bootstrap %>
<% end %>
